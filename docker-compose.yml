version: '3'
services:

  db:
    image: 'postgres'
    expose:
      - "5432"
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=leanmind
      - POSTGRES_PASSWORD=leanmind
      - POSTGRES_DB=leanmind
